import Layout from '../../components/layout';
import Head from 'next/head';

export default function FarmsHome(props) {
    return (
        <Layout {...props}>
            <Head>

            </Head>

            <ul>
                <li>Borum Farms API</li>
            </ul>
        </Layout>
    );
}