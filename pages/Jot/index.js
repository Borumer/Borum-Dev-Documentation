import Layout from '../../components/layout';
import Head from 'next/head';

export default function JotHome(props) {
    return (
        <Layout {...props}>
            <Head>

            </Head>

            <ul>
                <li>Borum Jot API</li>
            </ul>
        </Layout>
    );
}